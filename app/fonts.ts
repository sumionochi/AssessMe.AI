import { Inter, Montserrat, Lexend, Libre_Franklin } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const lexend = Lexend({
  subsets: ["latin"],
  display: "swap",
});

export const libreFranklin = Montserrat({
  subsets: ["latin"],
  display: "swap",
});
